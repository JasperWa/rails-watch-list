<%= simple_form_for(list) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, label: "List image", as: :file %>
  <div class="btn-center">
      <%# <%= f.button :submit %>
      <%= button_tag(type: 'submit') do %>
        <span><i class="fa-solid fa-clapperboard"></i>  Add list</span>
      <% end %>
    </div>
<% end %>
